import { User } from './User';

export interface RegisteredUser {
    registeredid: number
    favoriteFood: string
    favoriteColor: string
    favoritePet: string
    zodiacSign: string
    favoritePalindrome: string
    myUser: User
}